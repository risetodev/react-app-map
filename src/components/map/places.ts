import { IPlace } from "./types";

export const PLACES: IPlace[] = [
  {
    id: null,
    name: null,
    location: { lat: null, lng: null },
    vicinity: null
  }
];

const getPlaces = (place: string) => {};
